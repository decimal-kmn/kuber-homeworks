apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-back-app-svc
spec:
  selector:
    app: {{ .Release.Name }}-back-app
  ports:
  - port: {{ .Values.servicePort }}
    targetPort: 80
